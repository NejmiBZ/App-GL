/**
 * MODUL 3: KONFIGURATION — Zentrale Konstanten
 */
export const CONFIG = {
  SNAP_RADIUS: 35,          // Magnetpunkt-Radius in Canvas-Pixeln
  DEFAULT_ZOOM: 0.75,       // Standard-Zoom beim Upload
  MAX_HISTORY: 60,          // Maximale Undo-Schritte
  MAX_DISPLAY_WIDTH: 900,   // Maximale Anzeige-Breite eines Canvas-Bildes
  SPLITTER_SIZE: 120,       // Standard-Splitter-Rechteck-Größe
  PAGE_NUMBER_OFFSET: 3,    // Bilder 1-3 keine Seitennummer; Bild 4 zeigt "1"
  EXPORT_QUALITY: 1.0,      // JPG-Qualität für Export (immer HD)
};
